export interface IImageProps {
  src: string; // The file path for the image
  height: string; // Height of the image (e.g., "100px" or "50%")
  width: string; // Width of the image (e.g., "100px" or "50%")
  className?: string;
}
