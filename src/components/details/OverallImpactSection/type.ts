export interface IOverallImpactSectionProps {
  config: {
    imgSrc: string;
    title: string;
    description: {
      prefixText: string;
      highlightedText: string;
      suffixText: string;
    };
  };
}
